[project]
name = "Trading-Core"
version = "0.1.0"
description = "High-frequency trading core."
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-dotenv",
    "requests",
    "websockets",
    "psutil",
]
